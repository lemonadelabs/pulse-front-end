{{#if showNotification}}
  <div class="notifcation-alert fancy-corners">
    <p class='alert-message'>{{alertMessageStringSafe}}</p>
    {{#if undoableAlert}}
      <button>Undo</button>
    {{/if}}
  </div>
{{/if}}
